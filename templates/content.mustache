<div class="dashboard-flex">

  <div class="dashboard-left">
    <h4>📅 Événements à venir</h4>
    {{#calendarevents}}
      <div class="event-card">
        <strong>{{title}}</strong><br>
        <small>{{date}}</small>
        <div>{{{description}}}</div>
      </div>
    {{/calendarevents}}
    {{^calendarevents}}
      <p>Aucun événement à venir.</p>
    {{/calendarevents}}
  </div>

  <div class="dashboard-right">
    <h4>📘 Cours qui commencent aujourd’hui</h4>
    {{#hascourses}}
      <ul class="today-courses">
        {{#todaycourses}}
          <li><a href="{{url}}">{{fullname}}</a></li>
        {{/todaycourses}}
      </ul>
    {{/hascourses}}
    {{^hascourses}}
      <p>Aucun cours aujourd’hui.</p>
    {{/hascourses}}
  </div>
  
</div>

<div class="dashboard-blogs">
  <h4>📰 Derniers articles</h4>
  {{#hasblog}}
  <div class="blog-grid">
    {{#blogposts}}
    <div class="blog-card">
      <img src="{{image}}" alt="{{title}}">
      <div class="blog-content">
        <h5>{{title}}</h5>
        <p>{{{description}}}</p>
        <a href="{{url}}" class="btn-readmore">Lire la suite ➜</a>
      </div>
    </div>
    {{/blogposts}}
  </div>
  {{/hasblog}}
  {{^hasblog}}
    <p>Aucun article de blog disponible.</p>
  {{/hasblog}}
</div>
